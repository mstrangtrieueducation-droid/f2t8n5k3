document.querySelectorAll('[data-view]').forEach(button=>button.addEventListener('click',()=>{const target=button.dataset.view;document.querySelectorAll('[data-view]').forEach(item=>item.classList.toggle('active',item===button));document.querySelector('#clean').hidden=target!=='clean';document.querySelector('#answers').hidden=target!=='answers';window.scrollTo({top:document.querySelector('.view-tabs').offsetTop,behavior:'smooth'});}));